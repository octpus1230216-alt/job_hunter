# 默认配置（不含任何密钥）。冻结版首次运行会自动复制本文件为 config.yaml。
# 之后在「⚙️ 配置」页填写你的 DeepSeek Key，或直接编辑本文件。
llm:
  deepseek:
    api_key: ""                # 在「⚙️ 配置」页填写，或在此留空后在 UI 填
    base_url: https://api.deepseek.com
    model: deepseek-chat
  provider: deepseek

internal:
  calibration_mode: false

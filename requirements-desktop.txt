# 桌面版冻结所需依赖（精简版，不含可选重型原生依赖）。
# 与 requirements.txt 的差异：
#   - 排除 jobspy / tls_client / playwright / ollama（均为“函数内懒加载”的可选功能，
#     缺失时仅对应高级抓取/本地模型不可用，核心功能不受影响）。
#   - 新增 pyinstaller（打包工具）。
streamlit>=1.28.0
markdownify>=0.4.0
regex>=2024.0.0
openai>=1.0.0
PyPDF2>=3.0.0
python-docx>=0.8.11
pyyaml>=6.0
jinja2>=3.1.0
beautifulsoup4>=4.12.0
httpx>=0.25.0
pandas>=2.0.0,<3.0.0
numpy>=2.0.0
lxml>=5.0.0
requests>=2.28.0
rich>=13.0.0
pyinstaller>=6.0.0
